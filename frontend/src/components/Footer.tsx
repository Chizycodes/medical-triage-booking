const Footer = () => {
	return (
		<footer className="text-center py-6 text-text-body text-sm">
			MediTriage &copy; {new Date().getFullYear()} - For demo purposes only.
		</footer>
	);
};

export default Footer;
